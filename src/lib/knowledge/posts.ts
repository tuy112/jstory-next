import { KnowledgeItem } from './types/knowledgeItem';

export const knowledgePosts: KnowledgeItem[] = [
    {
        id: 1,
        topic: 'DB/SQL(ORACLE)',
        title: '과거의 Oracle(11c등)이 Oracle 19c와 다른 점',
        subtitle: 'Oracle9i에 익숙한 개발자를 위한 CDB/PDB 기초',
        content:
            `## 왜 이 개념이 낯설게 느껴질까요?

Oracle 9i, 10g, 11g 시절에는 "인스턴스 1개 = 데이터베이스 1개"가 당연했습니다. 서버에 DB를 하나 설치하면 그게 전부였고, DB를 하나 더 쓰려면 인스턴스를 통째로 하나 더 띄워야 했습니다. 그래서 이 시절 아키텍처에 익숙한 분들은 12c부터 등장한 CDB/PDB 구조를 처음 보면 낯설게 느껴질 수밖에 없습니다.

## 기존 구조 (Non-CDB, ~11g)

- 인스턴스(메모리 + 백그라운드 프로세스) 1개가 데이터베이스 1개만 담당
- DB를 3개 운영하려면 인스턴스도 3개, SGA도 3배, 백그라운드 프로세스도 3배 필요
- 서버 자원을 많이 잡아먹고, 패치나 업그레이드도 DB마다 각각 진행해야 함

## CDB / PDB란 무엇인가

12c부터 오라클은 이 구조를 "그릇"과 "내용물"로 분리했습니다.

- CDB (Container Database) : 인스턴스와 공용 자원(메모리, 백그라운드 프로세스, 컨트롤파일, 리두로그)을 갖고 있는 큰 틀
- PDB (Pluggable Database) : 그 틀 안에 꽂아 넣는 실제 업무용 데이터베이스. 테이블, 유저, 데이터파일 등 실질적인 데이터가 여기 들어있음

비유하자면 CDB는 아파트 건물이고, PDB는 그 안의 각 세대입니다. 건물의 전기/수도/엘리베이터 같은 공용 설비는 하나만 있으면 되고, 세대마다 각자의 생활공간만 따로 있는 구조와 같습니다.

## 왜 이렇게 나눴나

- 자원 절약 : SGA, 백그라운드 프로세스를 여러 PDB가 공유하므로 서버 한 대에서 여러 DB를 훨씬 가볍게 운영 가능
- 관리 일원화 : 패치나 업그레이드를 CDB 하나에만 적용하면 그 안의 모든 PDB에 한번에 반영됨
- 이식성 : PDB는 unplug/plug 방식으로 다른 CDB로 통째로 옮기거나 복제할 수 있어 마이그레이션과 클론 작업이 훨씬 간편해짐

## 11c 시절 개발자가 가장 헷갈리는 부분

1. 접속하면 기본적으로 CDB의 ROOT 컨테이너에 접속됩니다. 예전처럼 SQL*Plus로 접속해서 바로 테이블을 조회하면 아무것도 안 보이는 경우가 있는데, PDB 안이 아니라 ROOT에 있기 때문입니다.

\`\`\`
-- 현재 어느 컨테이너에 있는지 확인
show con_name;
-- PDB로 전환
alter session set container = pdb1;
\`\`\`

2. 유저 체계가 두 종류로 나뉩니다.
- Common User : CDB 전체(ROOT + 모든 PDB)에서 공통으로 존재하는 유저. 이름 앞에 반드시 C##을 붙여야 함 (예: C##ADMIN)
- Local User : 특정 PDB 안에서만 존재하는, 예전 방식과 동일한 일반 유저

3. 권한을 줄 때도 계층이 생깁니다. ROOT에서 준 권한이 자동으로 모든 PDB에 적용되지 않아, PDB 안에서 다시 권한을 부여해야 하는 상황이 자주 발생합니다.

## 19c에서 특히 달라진 점

- 19c부터는 신규 설치 시 기본값이 CDB 구조이며, Non-CDB 설치는 사실상 지양되는 추세입니다.
- Standard Edition 2 기준으로 19c부터는 별도 라이선스 없이 PDB를 최대 3개까지 만들 수 있게 완화되었습니다.
- PDB 단위 스냅샷, 리프레시 기능이 강화되어 테스트 환경 복제와 롤백이 더 쉬워졌습니다.
- V$CONTAINERS, V$PDBS, CDB_PDBS 같은 뷰로 컨테이너 상태를 더 세밀하게 조회할 수 있습니다.

## 한 줄 정리

11c까지는 "DB 하나 = 인스턴스 하나"였다면, 12c 이후(그리고 19c에서 사실상 표준이 된) 구조는 "인스턴스(CDB) 하나 안에 여러 개의 실질적인 DB(PDB)를 꽂아 쓰는 방식"입니다. 접속 후에는 내가 지금 ROOT에 있는지 특정 PDB에 있는지부터 확인하는 습관을 들이는 게 이 아키텍처에 적응하는 가장 빠른 방법입니다.`,
        createdAt: '2026-07-16',
    },
    {
        id: 2,
        topic: 'DB/SQL(ORACLE)',
        title: 'DB 캐릭터셋 문제 해결',
        subtitle: 'ojdbc8부터 orai18n이 빠진 이유와 의존성으로 해결하는 법',
        content:`## 어떤 에러인가

애플리케이션에서 Oracle DB에 접속해 쿼리를 날렸는데 아래 같은 예외가 발생하는 경우가 있습니다.

\`\`\`
java.sql.SQLException: 지원되지 않는 문자 집합(클래스 경로에 orai18n.jar 추가): KO16MSWIN949
\`\`\`

DB 설정이 잘못된 것처럼 보이지만, 실제로는 애플리케이션(클라이언트) 쪽 JDBC 드라이버가 문제인 경우가 대부분입니다.

## KO16MSWIN949이란

한글 윈도우 환경에서 흔히 쓰이는 완성형 확장 문자셋입니다. 기존 KO16KSC5601은 국가표준(KSC5601)에 정의된 한글만 지원해서 '똠', '샾' 같은 일부 조합형 한글이 깨지는 문제가 있었는데, KO16MSWIN949은 KO16KSC5601의 상위 집합으로 이런 조합 가능한 현대 한글을 추가로 지원합니다. 그래서 국내 레거시 시스템에서 한글 깨짐 문제를 해결하려고 DB의 NLS_CHARACTERSET을 KO16MSWIN949로 설정해둔 경우가 많습니다.

## 왜 JDBC에서 에러가 나나

Oracle JDBC 드라이버(ojdbc.jar)가 기본적으로 지원하는 문자셋 목록에는 유니코드 계열 등 표준 문자셋만 포함되어 있고, KO16MSWIN949 같은 확장 문자셋은 빠져있습니다. 이 문자셋을 다루려면 Oracle이 별도로 제공하는 orai18n.jar가 클래스패스에 있어야 합니다. 즉 DB는 정상인데 클라이언트가 그 문자셋을 "모르는" 상태인 겁니다.

## 해결 방법 1: orai18n.jar 직접 추가

Oracle에서 orai18n.jar 파일을 받아 프로젝트의 lib 폴더에 넣고, 클래스패스(또는 실행 스크립트)에 등록합니다.

\`\`\`
set jdbc_lib=.\\lib\\ojdbc8.jar;.\\lib\\orai18n.jar
\`\`\`

## 해결 방법 2: 빌드 도구 의존성으로 추가 (권장)

Gradle이나 Maven을 쓴다면 의존성을 하나 추가하는 게 더 깔끔합니다.

\`\`\`
// build.gradle
implementation 'com.oracle.ojdbc:orai18n:19.3.0.0'
\`\`\`

\`\`\`
<!-- pom.xml -->
<dependency>
  <groupId>com.oracle.ojdbc</groupId>
  <artifactId>orai18n</artifactId>
  <version>19.3.0.0</version>
</dependency>
\`\`\`

## 주의할 점: ojdbc8부터는 기본 미포함

ojdbc6, ojdbc7까지는 orai18n 관련 클래스가 드라이버 안에 일부 포함되어 있었지만, ojdbc8 이상부터는 이 스펙이 빠졌습니다. 그래서 최신 ojdbc8/ojdbc11을 쓰면서 이 에러를 만났다면, 드라이버를 구버전으로 내리는 임시방편보다는 orai18n 의존성을 명시적으로 추가하는 방법이 안전합니다.

## 정리

1. 에러 원인은 DB 설정이 아니라 클라이언트(JDBC) 쪽에 KO16MSWIN949를 처리할 orai18n 클래스가 없어서
2. ojdbc8부터는 orai18n이 드라이버에서 빠졌으므로 별도 추가 필요
3. jar 파일을 직접 넣거나, Gradle/Maven 의존성으로 추가하면 해결
4. DB의 NLS_CHARACTERSET 자체를 바꾸는 건 훨씬 더 큰 작업(데이터 재적재 필요)이므로, 이 에러만으로 문자셋을 바꿀 필요는 없음`,
        createdAt: '2026-07-16',
    },
];